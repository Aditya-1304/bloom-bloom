[package]
name = "bloom-bloom"
version = "0.1.0"
edition = "2024"

[dependencies]
rayon = "1.12.0"
xxhash-rust = { version = "0.8.15", features = ["xxh3"] }

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
panic = "abort"

[features]
default = []
prefetch = []